{
  "type": "module",
  "scripts": {
    "dev": "esbuild source/main.ts --bundle --minify --sourcemap --outfile=dist/main.js --watch",
    "build": "esbuild source/main.ts --bundle --minify --sourcemap --outfile=dist/main.js"
  }
}
